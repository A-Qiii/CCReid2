提出基于大模型先验与可学习软令牌的“混合提示学习（Hybrid Prompt Learning）”机制： 指出現有 CC-ReID 方法要么像 CLIP-ReID 一样完全放弃显式文本
，要么受限于显式粗粒度文本导致性能下降。本课题创新性地将 Qwen2 生成的显式宏观属性（如颜色、款式）作为语义初始化锚点，并串联可学习特征令牌 [X]，既利用了 VLM 的先验知识，又通过对比学习让 [X] 吸收了细粒度视觉差异。

【核心架构与模型底座】
骨干网络：图像编码器（Image Encoder）使用 CLIP 预训练的 ViT-B/16；文本编码器（Text Encoder）使用 CLIP 预训练的 Transformer
。
输入特征维度：视觉特征 F 
ori
​
  和文本特征提取后，均需通过线性投影层（Projection Layer）映射至 512 维，并强制进行 L2 归一化，使得所有计算均在单位超球面上进行
。
【阶段一：多信息混合提示学习（Stage 1: Prompt Learning）】 设计目的：解决大模型硬文本（Hard Text）粗粒度导致的语义同质化和信息瓶颈问题。利用对比学习在冻结的视觉空间中，为每个身份和衣服撑开细粒度的语义锚点。 详细要求：
冻结机制：严格冻结图像编码器和文本编码器的所有参数
。
混合提示词构建：引入长度为 M=4 的可学习向量（nn.Parameter）
。
身份提示词（Identity Prompt）："A photo of a [Qwen_Macro_ID], [X] 
1
p
​
 [X] 
2
p
​
 [X] 
3
p
​
 [X] 
4
p
​
  person." 其中 [Qwen_Macro_ID] 是外部传入的字符串常量（如“男性，微胖”），[X]^p 是该身份独有的可学习张量
。
衣着提示词（Clothing Prompt）："A photo of a [Qwen_Macro_Cloth], [X] 
1
c
​
 [X] 
2
c
​
 [X] 
3
c
​
 [X] 
4
c
​
  clothes."
。
优化目标：仅优化这批可学习的 [X] 张量。使用 InfoNCE 形式的图文对比损失（Image-to-Text L 
i2t
​
  和 Text-to-Image L 
t2i
​
 ）分别对身份和衣着提示词进行优化
。
超参数要求：训练 120 个 Epochs，使用 Adam 优化器，学习率设为 3.5e-4
。第一阶段结束后，将所有优化好的文本特征固化保存，供第二阶段使用
。
【阶段二：视觉特征解耦与微调（Stage 2: Visual Feature Disentanglement）】 设计目的：利用第一阶段学到的精准高维文本锚点，引导视觉网络提取身份不变特征，同时像“外科手术”一样精准切除视觉特征中的衣服纹理，而绝不误伤行人的 3D 骨架体型。 详细要求：
冻结机制：严格冻结文本编码器和第一阶段固化的所有文本特征。放开图像编码器（ViT）和投影层进行微调
。
基础度量学习损失（Base Loss）：
分类损失 (L 
ce
​
 )：使用带标签平滑（Label Smoothing）的交叉熵损失，约束视觉特征具备基础全局判别力
。
三元组损失 (L 
tri
​
 )：使用带有难样本挖掘（Hard Sample Mining）的三元组损失，Margin 严格设定为 0.3，以构建健康的度量空间
。
图文身份引导损失 (L 
Guide
​
 )： 计算视觉特征与对应身份的固化文本特征之间的 Image-to-Text 交叉熵损失（对比全集所有的文本中心，而非仅 Batch 内部），将视觉特征向精准的语义锚点牵引
。
【核心】外科手术级衣着排斥（Clothing Feature Minimization）： 绝对禁止使用 KL 散度进行全局排斥。必须严格按照以下步骤实现基于 MIPL 的局部精细解耦：
步骤 A（提取该件衣服特征）：引入一个可学习的线性投影矩阵 Proj 
c
​
 。将原始视觉特征 F 
ori
​
  通过 Proj 
c
​
  映射得到当前的衣服映射特征 F 
img2clo
​
 
。
步骤 B（语义剥离校验）：计算 F 
img2clo
​
  与该图片真实的“衣着文本特征”之间的均方误差一致性损失 L 
sc
​
 （或交叉熵损失），确保 F 
img2clo
​
  里提取的确实是纯粹的衣服纹理，而不是身体骨架
。
步骤 C（截断余弦正交切除）：将原始视觉特征 F 
ori
​
  与提纯后的衣服特征 F 
img2clo
​
  进行正交推开。数学公式严格定义为：L 
de
​
 =max{0,cos(F 
ori
​
 ,F 
img2clo
​
 )}。通过 ReLU 截断，保证当夹角 ≥ 90度时不再惩罚，实现精准停机
。
第二阶段总损失： L 
Stage2
​
 =L 
ce
​
 +L 
tri
​
 +L 
Guide
​
 +L 
sc
​
 +L 
de
​
 
。
超参数要求：
训练 40-60 个 Epochs。
ViT 骨干网络的学习率（Base LR）极小化，设定为 5e-6。必须有 10 个 Epoch 的 Warmup 阶段（从 5e-7 线性预热）
。
学习率在第 20 和 30（或 30 和 50）个 Epoch 进行余弦或阶梯衰减
。
采样策略：BatchSize=64 (P=16 身份 × 每个身份 K=4 张图)
。